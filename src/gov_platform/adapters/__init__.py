"""Adapter plugin surface — architecture §6.

M0 defines the port (`Adapter`) and ships exactly one reference
implementation (`SyntheticAdapter`) to prove it. Plugin discovery,
sandboxing, and the draft/shadow/production promotion lifecycle are M3.
"""
