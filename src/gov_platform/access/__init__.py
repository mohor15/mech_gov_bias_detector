"""Access control — architecture (no numbered section; see
`docs/milestones/M13.md` Sourcing note), M13.

`Principal`/token-based authentication and authorization. Deliberately
named `access/`, not `auth/` — the latter sits immediately adjacent,
alphabetically and visually, to the existing `audit/` package (a
realistic source of a mis-typed import); see
`docs/milestones/M13.md` §5.1 / Revision note item 10.
"""
