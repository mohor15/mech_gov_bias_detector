"""Governance Engine — architecture §8.

M0 scope: wrap exactly one Policy's Finding into a Verdict. Policy Bindings,
multi-policy aggregation and disagreement surfacing, escalation rules, and
cryptographic signing are M4/M5 — see `governance_engine.engine` for the
precise boundary.
"""
