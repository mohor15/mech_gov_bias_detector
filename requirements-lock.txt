# Pinned, exact resolution of `pip install -e ".[dev]"` against pyproject.toml.
# pyproject.toml keeps loose `>=` ranges for library compatibility.
# Deliberately excludes the local editable install itself
# (`--exclude-editable`) — a portable lockfile shouldn't pin a local path.
# For a reproducible environment:
#   pip install -r requirements-lock.txt && pip install -e . --no-deps
# Regenerate after any pyproject.toml dependency change. Generated from
# `pip freeze` against this project's working venv, hand-filtered to remove
# unrelated packages left over from that venv's history (numpy, ollama,
# pandas, python-dateutil, six, tzdata — none imported by gov_platform;
# confirmed via `grep -r` for each name across src/) rather than a fresh
# clean venv this time around, per instruction during the M1 session.
annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.14.2
ast_serialize==0.6.0
certifi==2026.7.22
cffi==2.1.0
cfgv==3.5.0
click==8.4.2
colorama==0.4.6
coverage==7.15.2
cryptography==49.0.0
distlib==0.4.3
fastapi==0.140.0
filelock==3.32.0
greenlet==3.5.4
h11==0.16.0
httpcore==1.0.9
httptools==0.8.0
httpx==0.28.1
identify==2.6.19
idna==3.18
iniconfig==2.3.0
librt==0.13.0
mypy==2.3.0
mypy_extensions==1.1.0
nodeenv==1.10.0
packaging==26.2
pathspec==1.1.1
platformdirs==4.11.0
pluggy==1.6.0
pre_commit==4.6.1
psycopg==3.3.4
psycopg-binary==3.3.4
pycparser==3.0
pydantic==2.13.4
pydantic-settings==2.14.2
pydantic_core==2.46.4
Pygments==2.20.0
pytest==9.1.1
pytest-cov==7.1.0
python-discovery==1.5.0
python-dotenv==1.2.2
PyYAML==6.0.3
ruff==0.16.0
SQLAlchemy==2.0.51
starlette==1.3.1
typing-inspection==0.4.2
typing_extensions==4.16.0
uvicorn==0.51.0
virtualenv==21.7.0
watchfiles==1.2.0
websockets==16.1.1
